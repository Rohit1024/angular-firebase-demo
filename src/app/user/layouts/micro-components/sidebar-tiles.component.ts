import { Component } from '@angular/core';
import { RouterModule } from '@angular/router';

@Component({
  selector: 'sidebar-tiles',
  standalone: true,
  imports: [RouterModule],
  template: ``,
})
export class SideBarTilesComponent {}
